<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guichet_automatique">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Guichet_Automatique">
        <activity android:name=".EpargneActivity"></activity>
        <activity android:name=".ClientsActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ChequeActivity" />
        <activity android:name=".InteretActivity" />
        <activity android:name=".GuichetActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>